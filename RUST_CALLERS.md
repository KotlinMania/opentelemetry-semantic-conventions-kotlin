# Rust callers — `opentelemetry-semantic-conventions-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `opentelemetry-semantic-conventions` | `opentelemetry_semantic_conventions` | 6 | 3 | 3 | 2 |

## `opentelemetry-semantic-conventions`

### Inbound symbols (5 distinct, 6 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `opentelemetry-semantic-conventions`.

#### `opentelemetry_semantic_conventions` — 2 site(s) (2 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-otel` `src/metrics/client.rs:34` — `use opentelemetry_semantic_conventions as semconv`
- ✅ `codex-kotlin/codex-otel` `src/provider.rs:32` — `use opentelemetry_semantic_conventions as semconv`

#### `::opentelemetry_semantic_conventions` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `rama-core-kotlin/rama-core` `src/telemetry/opentelemetry/mod.rs:10` — `pub_use ::opentelemetry_semantic_conventions as semantic_conventions`

#### `opentelemetry_semantic_conventions::SCHEMA_URL` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `tracing-opentelemetry-kotlin/tracing-opentelemetry` `examples/opentelemetry-otlp.rs:7` — `use opentelemetry_semantic_conventions::SCHEMA_URL`

#### `opentelemetry_semantic_conventions::attribute::DEPLOYMENT_ENVIRONMENT_NAME` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `tracing-opentelemetry-kotlin/tracing-opentelemetry` `examples/opentelemetry-otlp.rs:7` — `use opentelemetry_semantic_conventions::attribute::DEPLOYMENT_ENVIRONMENT_NAME`

#### `opentelemetry_semantic_conventions::attribute::SERVICE_VERSION` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `tracing-opentelemetry-kotlin/tracing-opentelemetry` `examples/opentelemetry-otlp.rs:7` — `use opentelemetry_semantic_conventions::attribute::SERVICE_VERSION`
