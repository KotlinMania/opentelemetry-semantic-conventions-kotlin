# Port report — `opentelemetry-semantic-conventions-kotlin`

> Generated by `scaffold/generate_repo_reports.py` from `workspace_dep_graph.json` ⨯ `port_priority.json`. Do not hand-edit. Re-run after the workspace dep graph is rebuilt.

## Overview

- **1** member crate(s): `opentelemetry-semantic-conventions`
- **Codex-kotlin distance**: **hop 1** from codex-kotlin via cross-repo Rust deps
- **Inbound**: 6 `use` sites from 3 other repos
- **Outbound**: 0 `use` sites to 0 other repos
- **Unresolved external**: 0 published Cargo crates with no `*-kotlin/` port

## Member crates

| package | module | inbound `use` sites | outbound external repos | Jira | rank | status |
|---|---|---:|---:|---|---:|---|
| `opentelemetry-semantic-conventions` | `opentelemetry_semantic_conventions` | 6 | 0 | COD-90 | 46 | To Do |

## Inbound — what to port first

Every symbol this repo's crates need to expose, ranked by total workspace demand. **Codex-kotlin sites are listed first per symbol** because codex is the priority consumer driving the port queue. Anything not listed here is not currently `use`'d by any kotlinmania repo and can be deferred.

### Inbound caller summary (6 edges, 3 caller crates)

| caller repo | caller crate | use sites | codex consumer? |
|---|---|---:|---|
| `tracing-opentelemetry-kotlin` | `tracing-opentelemetry` | 3 |  |
| `codex-kotlin` | `codex-otel` | 2 | ✅ |
| `rama-core-kotlin` | `rama-core` | 1 |  |

### Symbol-level demand (5 distinct symbols)

Sorted by codex demand, then total demand. Symbols with codex-kotlin sites are the priority queue. Each symbol entry below shows up to 12 call sites (codex first).

| symbol | total | codex | other | distinct callers |
|---|---:|---:|---:|---:|
| `opentelemetry_semantic_conventions` | 2 | 2 | 0 | 1 |
| `::opentelemetry_semantic_conventions` | 1 | 0 | 1 | 1 |
| `opentelemetry_semantic_conventions::SCHEMA_URL` | 1 | 0 | 1 | 1 |
| `opentelemetry_semantic_conventions::attribute::DEPLOYMENT_ENVIRONMENT_NAME` | 1 | 0 | 1 | 1 |
| `opentelemetry_semantic_conventions::attribute::SERVICE_VERSION` | 1 | 0 | 1 | 1 |

#### Per-symbol detail (top 5 symbols)

##### `opentelemetry_semantic_conventions` (2 sites, 2 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-otel` `src/metrics/client.rs:34` — `use opentelemetry_semantic_conventions as semconv`
- `codex-kotlin/codex-otel` `src/provider.rs:32` — `use opentelemetry_semantic_conventions as semconv`

##### `::opentelemetry_semantic_conventions` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `rama-core-kotlin/rama-core` `src/telemetry/opentelemetry/mod.rs:10` — `pub_use ::opentelemetry_semantic_conventions as semantic_conventions`

##### `opentelemetry_semantic_conventions::SCHEMA_URL` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `tracing-opentelemetry-kotlin/tracing-opentelemetry` `examples/opentelemetry-otlp.rs:7` — `use opentelemetry_semantic_conventions::SCHEMA_URL`

##### `opentelemetry_semantic_conventions::attribute::DEPLOYMENT_ENVIRONMENT_NAME` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `tracing-opentelemetry-kotlin/tracing-opentelemetry` `examples/opentelemetry-otlp.rs:7` — `use opentelemetry_semantic_conventions::attribute::DEPLOYMENT_ENVIRONMENT_NAME`

##### `opentelemetry_semantic_conventions::attribute::SERVICE_VERSION` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `tracing-opentelemetry-kotlin/tracing-opentelemetry` `examples/opentelemetry-otlp.rs:7` — `use opentelemetry_semantic_conventions::attribute::SERVICE_VERSION`

## Outbound — what this repo depends on

Other kotlinmania repos this repo's Rust source `use`s. Symbol-level detail per target lives in **that target repo's PORT_REPORT.md inbound section** (no duplication). Sorted by Jira rank, then by `use`-site count.

_No outbound edges to other workspace repos._

## Unresolved external dependencies

Published Cargo crates this repo's Rust source `use`s but for which no `<crate>-kotlin/` repo exists. Each is either a missing port to file in Jira, or an intentional external dep (system library / FFI / build-only). If a port lives at a non-conventional repo name, add it to `PACKAGE_REPO_OVERRIDES` in `scaffold/build_workspace_dep_graph.py`.

_None — every external dep has a `*-kotlin/` port._
